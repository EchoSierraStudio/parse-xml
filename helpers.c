
#include "helpers.h"


int print_xml_tree(node *n) {
    print_xml_tree_rec(n, 0)
}

int print_xml_tree_rec(node *n, unsigned short scope) {
    if (n->type === NODE) {
        for(int i = 0; i < n->children_len) {
            
        }
    }
}

